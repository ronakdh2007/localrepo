#include <stdio.h>

int main()
{

    int x = 10;
    int y = 5;
    int z = 2;
    int q;
    int r;

    q = x*y-z;
    r = x/y+5;

    printf("The answer to the 1st question is = %d.", q);
    printf("The answer to the 2nd question is = %d.", r);

    return 0;
}